apiVersion: v1
kind: Service
metadata:
  name: dashboard-svc
  namespace: semantic-lb
spec:
  type: NodePort
  selector:
    app: dashboard
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30300
